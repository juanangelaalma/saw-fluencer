---
description: Run make targets.
agent: build
---

Usage: `make <target> [options...]`

Executes `make <target> [options...]` in the workspace root.

**Examples:**

```bash
make up
make down
make migrate
make seed
make format
```

for more targets you can access `Makefile`